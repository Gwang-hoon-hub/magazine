server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # DB Source
    url: jdbc:mysql://springboot-db.cyvpzdbrywmx.ap-northeast-2.rds.amazonaws.com:3306/magazine
    # DB username
    username: admin
    # DB password
    password: rhkdgns92
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
